language: node_js

node_js:
  - "10"

cache:
  npm: true

install: npm install

script:
  - npm test

deploy:
  - provider: script
    skip_cleanup: true
    script: npm run deploy:ropsten

  - provider: script
    skip_cleanup: true
    script: npm run deploy:mainnet
    on:
      tags: true